---
export type Props = {
  selected: "ru" | "en";
};
const { selected } = Astro.props;
---

<div>
  <a class={selected === "en" ? "active" : ""} href="/en/">EN</a>
  <span>{" / "}</span>
  <a class={selected === "ru" ? "active" : ""} href="/">RU</a>
</div>
<style>
  span {
    color: white;
  }
  a {
    font-family: Ysabeau;
  }
  .active {
    font-weight: 700;
    text-decoration: underline;
  }
</style>
